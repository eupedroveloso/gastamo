/** Proporção aproximada do cartão ISO/IEC 7810 ID-1 (85,60 × 53,98 mm) — ícone horizontal. */
export const CARD_ASPECT_RATIO = 85.6 / 53.98;

/** Miniaturas em listas/dashboard: arredondamento mínimo (evita formato “pílula”). */
export const CARD_THUMBNAIL_BORDER_RADIUS = 4;

/** Modal de recorte e painéis grandes. */
export const CARD_IMAGE_BORDER_RADIUS = 16;
